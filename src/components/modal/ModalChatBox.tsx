import React from 'react';

interface ModalChatBoxProps {
    showModal: boolean,
    setShowModal: any
}

const ModalChatBox = (props: ModalChatBoxProps) => {
    const { showModal, setShowModal } = props
    return (
        <>

        </>
    );
};

export default ModalChatBox;